{
  "name": "blog-roles-perms",
  "version": "1.0.0",
  "private": true,
  "description": "Blog application with roles and permissions",
  "scripts": {
    "test": "pnpm -r --workspace-concurrency=1 test:run",
    "test:watch": "pnpm -r test",
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
